import React from 'react'
import { Toolbar as MatToolbar } from '@material-ui/core'
import Typography from '@material-ui/core/Typography'

const Toolbar = () => (
  <MatToolbar>
    <Typography variant="subtitle1">Pomodor</Typography>
  </MatToolbar>
)

export { Toolbar }
